# --- Core data handling ---
pandas>=2.2,<3.0
numpy>=1.26,<2.0
openpyxl>=3.1,<4.0          # .xlsx read/write for CRM export ingestion
pydantic>=2.7,<3.0          # data models / validation for lead records

# --- HTTP / API clients ---
requests>=2.32,<3.0
httpx>=0.27,<1.0            # async HTTP for concurrent enrichment calls
tenacity>=8.3,<9.0          # retry/backoff for flaky third-party APIs

# --- Web scraping (company website signal extraction) ---
beautifulsoup4>=4.12,<5.0
lxml>=5.2,<6.0
playwright>=1.44,<2.0       # headless browser for JS-rendered sites

# --- Enrichment / CRM integrations ---
simple-salesforce>=1.12,<2.0
hubspot-api-client>=8.2,<9.0
setuptools<81               # hubspot-api-client's discovery module imports the
                             # deprecated pkg_resources, removed from newer setuptools

# --- LLM scoring & outreach drafting ---
anthropic>=0.34,<1.0

# --- Notifications ---
slack-sdk>=3.27,<4.0

# --- Licensing & billing (self-hosted sale) ---
pynacl>=1.5,<2.0            # Ed25519 offline license signing/verification
stripe>=15.0,<16.0          # seller-side Checkout Session + webhook

# --- Web backend (serves the React frontend's API) ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0  # required by FastAPI for file upload endpoints

# --- Scheduling (recurring enrichment/scoring runs) ---
apscheduler>=3.10,<4.0

# --- Config & logging ---
python-dotenv>=1.0,<2.0
loguru>=0.7,<1.0
