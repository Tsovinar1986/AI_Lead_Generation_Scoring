# Same-origin by default (see api.ts / vite.config.ts) -- works unmodified
# in both merged production mode and local dev (dev-server proxy). Only set
# this if the API genuinely lives on a different origin than the frontend.
VITE_API_BASE_URL=
